<div class="col-6 col-md-3 col-xl-2 my-3 acds-clickable skill-container d-flex flex-column" data-skill-id="{{skill_id}}"
    data-extra="{{extra_value}}">
    <div class="row">
        <div class="col">
            <input class="acds-element acds-bottom-text text-center skill-field w-75" type="text" value="{{value}}"
                maxlength="3" onchange="skillChange(event)" data-last-value="{{value}}">
        </div>
    </div>
    <div class="row mt-1">
        <div class="col" onclick="skillDiceClick(event)">
            <small class="total h5">{{total_value}}</small>
        </div>
    </div>
    <div class="row h-100">
        <div class="col name" onclick="skillDiceClick(event)">
            {{name}}
        </div>
    </div>
</div>