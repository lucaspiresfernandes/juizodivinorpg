<!DOCTYPE html>
<html>

<head>
    {{> head ignore_jquery=1 ignore_socketio=1 ignore_axios=1}}
    <meta charset="utf-8">
</head>

<body>
    <div class="container text-center">
        <div class="row my-4">
            <div class="col">
                <div class="row">
                    <div class="col h1">
                        {{rpgname}} RPG
                    </div>
                </div>
                <div class="row">
                    <div class="col my-3 h3">
                        Selecione o retrato que deseja acessar.
                    </div>
                </div>
                {{#each players}}
                {{#with this}}
                <div class="row">
                    <div class="col my-1">
                        <a href="/portrait/{{player_id}}" class="h5">{{value}} ({{player_id}})</a>
                    </div>
                </div>
                {{/with}}
                {{/each}}

            </div>
        </div>
    </div>
</body>

</html>